apply plugin: 'war'
apply plugin: 'jetty'

dependencies {
    providedCompile group: 'javax', name: 'javaee-api', version: '7.0'
}

jettyRun {
    httpPort = 8080
    stopPort = 8090
    stopKey = null
}
